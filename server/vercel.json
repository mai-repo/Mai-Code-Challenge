{
    "version": 2,
    "builds": [
      {
        "src": "server/main.py",
        "use": "@vercel/python",
        "config": {
          "maxDuration": 60
        }
      }
    ],
    "routes": [
      {
        "src": "/(.*)",
        "dest": "server/main.py"
      }
    ]
  }